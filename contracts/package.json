{
  "name": "@linepay/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.cjs --network arcTestnet",
    "deploy:local": "hardhat run scripts/deploy.cjs",
    "deploy:marketplace": "hardhat run scripts/deploy-marketplace.cjs --network arcTestnet",
    "deploy:marketplace:local": "hardhat run scripts/deploy-marketplace.cjs",
    "deploy:mock-usdc": "hardhat run scripts/deploy-mock-usdc.cjs --network arcTestnet",
    "deploy:mock-usdc:local": "hardhat run scripts/deploy-mock-usdc.cjs"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
